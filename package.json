{
  "name": "app-comptabilite-root",
  "version": "1.0.0",
  "scripts": {
    "build": "cd \"app de comptabilité/client\" && npm install && npm run build",
    "start": "cd \"app de comptabilité/server\" && npm install --production && node src/index.js"
  }
}
