{
  "name": "app-comptabilite-root",
  "version": "1.0.0",
  "scripts": {
    "build": "cd \"app de comptabilité/server\" && npm install && cd ../client && npm install && npm run build",
    "start": "node server.js"
  }
}
